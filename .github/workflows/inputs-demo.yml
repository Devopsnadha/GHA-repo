name: Inputs Demo
on:
  workflow_dispatch: 
    inputs:
      environment:
        description: "choose the environment to deploy"
        required: true
        default: "dev"
        options:
         - dev
         - test
         - prod
      artifact_tag:
       description: choose the tag to deploy
       required: true
jobs:
 input-demo:
  runs-on: ubuntu-latest
  steps: 
    - name: deployb the app
      run: echo "deploying the ${{ inputs.artifact_tag }} application to ${{ inputs.environment }}"
