name: Env Demo
on: workflow_dispatch
env:
  APP_ENV: "dev"
jobs:
  demo:
   env:
    APP_ENV: "test"
   runs-on: ubuntu-latest
   steps:
     - name: connecting to env
       env:
        APP_ENV: "test"
       run: echo "App connecting to $APP_ENV"
