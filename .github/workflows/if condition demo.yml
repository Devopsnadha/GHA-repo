name: 
on: 
 push:
  branches:
   - main
   - develop
jobs:
  if-demo:
   runs-on: ubuntu-latest
   steps:
    - name: run this step
      if: github.ref == "refs/heads/main"
      run: echo "Runing this step" 
